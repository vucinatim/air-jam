# Suggestions

Only keep live follow-ups here. Completed reset work and stale migration notes should not stay in this file.

## High-Impact Follow-Ups

1. Keep v1 public copy honest about the shared-screen phone-controller product, but keep the future docs explicit that distributed-screen play is a later mode so the roadmap stays understandable without overpromising.
2. Revisit the 4-character room-code entropy before or immediately after wider public rollout now that proxy trust is no longer the weak link and remote room sharing is a likely future product lane.
3. Design the future room participant/surface model before implementing distributed-screen clients, so host, controller, display, shell, and agent participants do not get bolted onto separate indices.
4. Design an optional display snapshot lane on top of the existing host-owned state model, with typed snapshots, reconnect replay, throttling, and display-surface subscriptions instead of automatic pixel mirroring.
5. Design a small distributed-screen input/binding contract for post-v1 remote-screen games and future agents, keeping it additive over today's phone-controller lane rather than replacing it.
6. Promote the current docs render validation to a built-route crawl so docs HTML is verified through actual route output instead of only the canonical render path.
7. Add RSS and Atom feeds for the new blog slice once the first real release/article cadence starts, so release posts can be subscribed to without scraping the site or overloading docs endpoints.
8. Consolidate host bootstrap verification into one dedicated auth middleware or bootstrap service boundary so static app ID mode, signed host-grant mode, and future managed-mode policy do not stay split across handlers.
9. Extract the parallel host/controller bridge clients into a small generic bridge transport base so handshake, timeout, port lifecycle, and event fanout do not stay duplicated.
10. Replace or reconfigure `air-capture`'s physics runtime so the host build no longer ships `@dimforge/rapier3d-compat` as a 2 MB+ JS chunk; the easy route/editor/template splits are already done, so the remaining warning is now a real physics-runtime cost rather than loose app structure.
11. Add optional causal message IDs for key cross-surface hops so some failure stories can be followed by direct cause/effect links instead of correlation by `traceId`, `roomId`, `collectorSeq`, and per-session `sourceSeq` alone.
12. Finish narrowing the remaining `air-capture` 3D runtime seams by moving projectile scene traversal, Rapier body lookup, and prefab sensor-sync details behind explicit engine/adapters seams so the runtime keeps converging toward pure-step helpers plus thin render adapters.
13. Move release moderation off the synchronous publish request path onto a small background job or workflow once real publish volume exists, so browser startup and third-party moderation latency do not stay coupled to the creator-facing publish click forever.
14. Rename the internal `games.url` column and related server field names to `preview_url` / `previewUrl` so the persisted data model matches the now-honest dashboard contract instead of carrying the old self-hosted naming forever.
15. Add a follow-on `GameSettingsRuntime` layered on top of the platform settings model so game-specific preferences can be schema-owned and namespaced without polluting the shared platform settings contract.
16. Revisit the platform dashboard auth surface and remove the email/password fallback if GitHub-first usage proves sufficient, so the public auth posture does not carry a knowingly secondary password UX longer than needed.
17. Revisit SDK package ergonomics only if it still materially helps adoption, but do it under the explicit runtime-ownership model rather than reopening the older headless-first singleton-owner direction.
18. Add optional per-action schema validation for networked store actions only if real game usage proves the zero-or-one plain-object RPC contract still needs stronger validation; keep it additive and narrow rather than turning the whole store layer into a schema-first framework.
19. Split scaffold validation into a fast contract smoke and a full catalog sweep so routine local verification does not always require the heaviest `pnpm test:scaffold` path.
20. Migrate workspace/repo command env parsing to `@air-jam/env` so runtime contract validation stays consistent beyond server/platform/create-airjam boundaries.
21. Extend local proof prebuild caching beyond `@air-jam/sdk` only if repeated browser or release-proof runs still spend material time rebuilding unchanged package/game work.
22. Revisit repeated host-side derived-state clusters only if they keep spreading across games, but solve them with tiny game-domain helpers or hooks rather than another config layer so host screens stay readable without hiding runtime ownership behind indirection.
23. Consider optional session-local preview workspace persistence for window positions and minimize state only if repeated real usage shows the current fresh-each-reload behavior slows local testing; keep it strictly host-local and do not let it leak into replicated game/runtime state.
24. Revisit `SurfaceViewport` preset baselines after a few more real-device and small-window passes, especially `host-standard`, so the shared full-bleed scaling model keeps its clean semantics while landing on more confident default reference dimensions.
25. If real usage still shows stale local preview controllers after abrupt iframe/tab teardown, consider adding the smallest reliable unload transport on top of the new explicit preview-close handshake, but keep it additive over the real phone/controller reconnect lease instead of collapsing the lease itself.
26. Replace Arcade's bespoke full-screen join QR overlay with the shared SDK `JoinQrOverlay` now that game lobbies use the same primitive, so QR presentation and dismissal behavior do not diverge between the platform shell and embedded host shells.
27. If host-side top-right utilities keep growing beyond mute, fullscreen, and preview controllers, promote the current host preview dock accessory slot into a small first-class `HostUtilityDock` contract so games register utility actions declaratively instead of drifting back toward ad hoc overlay chrome.
28. Run one repo-wide game-template naming pass so equivalent concerns stop drifting across folder names. Current examples: `pong` uses `src/ui` where most games use `src/components` and/or `src/game/ui`, and `last-band-standing` uses top-level `src/store` while most other games keep gameplay state under `src/game/stores`. Pick one convention and mirror it through scaffold sources.
29. Revisit The Office runtime ownership after the render-isolation refactor and decide whether player positions, pending tasks, and other per-frame runtime refs should graduate into an explicit host-side runtime snapshot contract for future agent tooling, instead of remaining hook-local mutable refs forever.
30. If another launch game needs the same hot-state isolation pattern, extract only the tiny shared pieces that proved reusable here, such as guarded record-merge helpers or narrow replicated-selector utilities, rather than promoting game-specific runtime ownership into a broader framework abstraction too early.
31. Once a second game has real unit prefabs, generalize the Air Capture prefab preview conventions into a broader game-owned prefab capture system so deterministic prefab screenshots/config variants stop depending on Air Capture-specific query conventions.
32. Once two or three games actually need it, extract a tiny shared scene-population contract for runtime spawn/pool layers that map state or authored layout into many prefab instances, but keep it separate from the prefab definition contract so pooling and population do not leak back into `src/game/prefabs/`.
33. If real launch traffic shows platform-only Sentry is not enough, add the smallest correct release error-tracking baseline for server and browser-runtime failure stories instead of assuming platform monitoring alone covers the whole product.
34. Once hosted release ingestion is ready for it, prefill and diff platform-submitted catalog fields from exported `gameMetadata` so the dashboard stays authoritative while code metadata remains useful for tooling and release artifacts; include the new developer-facing source/template fields in that prefill path.
35. Decide whether to keep or collapse the remaining lifecycle split between `packages/devtools-core/src/dev.ts` managed background processes and the repo CLI foreground runtime flow now that both paths already use the same shared runtime owners; if we keep both, document the policy boundary explicitly, and if we collapse them, do it by extracting one shared process-lifecycle contract instead of reintroducing script-owned orchestration.
36. Once two or three games use the new virtual-controller lane in anger, add the smallest shared contract for agent-facing controller sequences and richer game-owned runtime snapshot publication so agents stop inferring press/release choreography and store-domain meaning from source code alone.
37. Decide whether game-owned semantic agent contracts should also publish optional controller-sequence helpers or richer action preconditions once a second game adopts the new lane, so agents can stop inferring multi-step lobby/start/answer choreography from snapshots alone.
38. Revisit `last-band-standing`'s lobby-reset/player-lifecycle semantics after the first live semantic-MCP demos: `resetLobby` returns the room to lobby cleanly, but disconnected demo controllers currently remain in the lobby roster/state, which may be correct for resume semantics or may need a tighter host-owned cleanup rule.
39. Revisit Pong's post-demo lobby cleanup semantics after the first semantic-MCP QA pass: after `return_to_lobby` and controller disconnect, the host can still show a disconnected player in the lobby because the current player/resume lease semantics outlive the agent session. Decide whether that persistence is the right shared reconnect behavior or whether game-facing lobby contracts need a cleaner disconnected-player projection.
40. Broaden template adoption of the semantic agent contract only after deciding which shipped templates should expose it by default; scaffold packaging/docs now preserve and teach template-owned `src/game/contracts/agent.ts`, so the remaining work is selective template authorship, not another generator layer.
41. If first-party monorepo games need direct hosted-release publishing later, add one explicit repo-owned release builder instead of reviving path inference against the monorepo root; local `release_doctor` / `release_bundle` / `release_submit` are now intentionally standalone-project tools, so any future monorepo publish lane should declare its target game and output contract directly.
42. Run the external-agent acceptance test again against a fresh local `minimal` scaffold now that it ships `src/game/contracts/agent.ts`, the gold-path doc, and the host-action listener seam, then record the exact remaining confusion points before deciding whether Phase 4 is truly complete.
43. Add one first-class preflight for semantic session bootstrap browser requirements so `open_game_session` and hidden host ownership fail with a direct "install Playwright Chromium" style fix or automatically provision the needed browser, instead of surfacing a raw missing-executable stack from internal runtime helpers.
44. Add a tiny preview-managed lifecycle command set such as `airjam dev:preview status|stop` (or an equivalent `airjam dev --preview-managed --status|--stop`) so background local Air Jam servers do not turn into mystery port owners during repeated agent/browser sessions.
45. Collapse the platform docs/blog MDX render path onto one explicit runtime contract shared by Next build and Vitest so CI does not depend on fragile alignment between custom loader aliases, generated content imports, and React JSX runtime resolution details.
46. Canonicalize the platform deployment env contract into one typed module and prune implicit public/runtime fallbacks where practical, so `NEXT_PUBLIC_AIR_JAM_PUBLIC_HOST`, `BETTER_AUTH_URL`, `NEXT_PUBLIC_AIR_JAM_SERVER_URL`, and `NEXT_PUBLIC_AIR_JAM_APP_ID` stop being resolved from scattered helpers and become one explicit deploy identity surface; keep `hostGrantEndpoint` as an explicit SDK/app override instead of treating it like first-party platform deployment env.
47. Add an explicit protection model for the release browser worker websocket endpoint before depending on it more heavily in preview or production automation; today the worker proxies the Playwright websocket publicly without worker-side caller auth, so the long-term contract should choose provider-private networking, worker-side access control, or another narrow gate instead of leaving browser control to deployment topology luck.
48. Split platform-local development conveniences from the deployable platform contract more aggressively, so local Arcade reference-game env (`NEXT_PUBLIC_AIR_JAM_LOCAL_REFERENCE_*`), local proxy rewrites (`AIR_JAM_DEV_PROXY_BACKEND_URL`), and local-build routes stop living so close to the hosted product surface and reduce the chance of deploy-time env/config confusion.
49. Introduce one explicit preview/deployment configuration boundary shared by platform, server, and future preview automation so provider-specific names such as `VERCEL_URL`, `RAILWAY_STATIC_URL`, and `RAILWAY_PUBLIC_DOMAIN` do not stay embedded across multiple runtime helpers; the repo should compute one canonical app/server/worker identity contract and let the rest of the code consume that typed result.
50. Refactor `packages/server/src/index.ts` into a thinner composition root by extracting health/dev endpoints, auth/runtime wiring, and socket bootstrap composition into narrower infrastructure modules, so the server remains the canonical deployable runtime without also being the place where every operational concern is assembled inline.
51. Once the canonical bot runtime track begins implementation, add one small shared eval corpus and latency benchmark harness for bot observation -> intent -> controller loops on M1 Pro class hardware, so local-first claims stay grounded in repeatable measurements instead of intuition.
52. Keep the future bot-driver contract intentionally narrower than the semantic agent contract surface: only extract shared intent primitives once at least two real games need them, so Air Capture does not accidentally hardcode its gameplay semantics into the framework too early.
53. Revisit Railway selected-service sync only if Railway exposes a real automatable primitive for it; the 2026-05-06 validation showed that `serviceDuplicate` is not that primitive because it creates global `(... Copy)` services instead of syncing the canonical app services into a preview environment.
54. If real preview usage still leaks resources despite the destroy workflow and the scheduled sweeper, add the smallest possible diagnostics pass first, such as age-tagged stale-preview reporting in `preview doctor`, instead of introducing another preview lifecycle.

## Framework Boundary Follow-Ups

These are still useful post–Arcade reset, but they should be handled as small hardening passes rather than another migration.

1. **Single bootstrap owner:** Centralize embedded-vs-standalone runtime resolution and URL parsing into one module so room/join/arcade bootstrap does not stay split across runtime params, embedded adapters, realtime clients, and runtime hooks.
2. **Reconnect seam narrowing:** Revisit `hostArcadeRestore` and move it behind a host-only adapter if we can do so cleanly, so `AirJamStore` stays room/session shaped instead of carrying host-shell restore state forever.
3. **Protocol change recipe:** Funnel schema/protocol changes through `packages/sdk/src/protocol` more deliberately and document the change path in `docs/monorepo-operating-system.md` so transport updates stop fanning out loosely.
4. **Embedded crash-exit contract:** Add a first-class parent/child bridge contract for "exit embedded game after runtime crash" so iframe error fallback can request a clean Arcade return path without relying on manual host overlay exit.
