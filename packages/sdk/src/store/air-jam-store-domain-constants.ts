/** Replicated game / app state when a single default store is used (standalone or non-embedded). */
export const AIR_JAM_DEFAULT_STORE_DOMAIN = "default";
/** Arcade platform shell (`ArcadeSurfaceState`) — isolates from game `default` domain in the same room. */
export const AIR_JAM_ARCADE_SURFACE_STORE_DOMAIN = "arcade.surface";
