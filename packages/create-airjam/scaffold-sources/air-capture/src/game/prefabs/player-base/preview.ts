export const AIR_CAPTURE_PLAYER_BASE_PREVIEW = {
  summary: "A team base zone that scores captured flags and returns friendly flags.",
  accentColor: "#f97316",
  dimensions: {
    radius: 10,
    height: 10,
  },
} as const;
